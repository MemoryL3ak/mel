# URL base de la API en producción (backend en Railway), terminada en /api.
# En desarrollo no se necesita: el proxy de Vite apunta a localhost:4100.
VITE_API_URL=https://<servicio>.up.railway.app/api
