# Copiar a server/.env con las credenciales del proyecto Supabase
# (Project Settings → API Keys). La secret key solo vive en el servidor.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...
